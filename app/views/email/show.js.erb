$('p.email_object').text( '<%= (@email.object) %>');
$('p.email_body').text( '<%= (@body) %>');
$('p.email_destroy_btn').html('<%= link_to "Détruire", @email, class:"btn btn-danger", data: {confirm: "êtes-vous sûr ?"}, :remote => true, method: :delete %>');
$('#email_body').fadeIn();


$(".status").each(function() {
  if ($(this).attr('value') == "true") {
      $(this).css("background-color", "#d8d8d8");
  }; 
});

$('#<%= (@email.id) %>').parent().css("background-color", "#aaa");
$('#<%= (@email.id) %>').parent().attr("value", "true");